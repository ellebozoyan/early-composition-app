# Installing Early Composition on iPad

Unzip, then host the folder (Netlify Drop or GitHub Pages) and open in Safari -> Share -> Add to Home Screen. Keep the .nojekyll file. Works offline after first load.
